# Open Questions

Running log of unresolved decisions for **Block Converter for Divi**.

**How to use this file:** add new questions to the Open table with the next
sequential ID and the date raised. When a question is answered, move its row to
the Resolved section, record the resolution date, the decision, and where the
decision was implemented. Never delete a resolved question — the history of why
a decision was made is the point of the file.

**Priority:** `P1` blocks a release · `P2` needed soon · `P3` nice to settle

---

## Open

| ID | Raised | Pri | Area | Question |
| --- | --- | --- | --- | --- |
| Q5 | 2026-08-04 | P2 | Scope | Divi Theme Builder templates (headers, footers, global layouts) and Divi Library global modules are out of scope. Is that permanent, or a phase-2 deliverable? Without them a "converted" site still needs Divi active for its chrome. |
| Q7 | 2026-08-04 | P2 | Fidelity | Gallery carousels get a `d2g-gallery-slider` class with no shipped CSS or JS. Do we ship a small front-end asset to honour it, convert carousels to a plain grid gallery, or document the class as a theme integration point? |
| Q19 | 2026-08-04 | P2 | Distribution | `readme.txt` needs two things that cannot be authored from here: a `Contributors:` value that is a real WordPress.org username (currently the GitHub handle `johnjanney`, which may not exist there), and three screenshot PNGs to back the Screenshots section. Screenshots live in the SVN `assets/` directory, not the plugin ZIP. Who captures them? |
| Q11 | 2026-08-04 | P3 | Scope | Should there be a WP-CLI command for batch migration? Browser-driven batch conversion runs sequential AJAX calls and will be slow and timeout-prone on sites with hundreds of Divi pages. |
| Q14 | 2026-08-04 | P3 | Scope | Should the plugin offer to convert Divi custom post types beyond `page` and `post` — e.g. `project` (Divi Projects), or arbitrary CPTs a site has enabled the builder on? The scan is currently hardcoded to `('page','post')`. |
| Q26 | 2026-08-05 | P2 | Fidelity | A table containing `<caption>` or `<colgroup>` is now preserved whole as `core/html` rather than losing the caption, which is safe but not editable. `core/table` does model a caption (a `<figcaption>` inside the `<figure>`). Do we map captions natively — and accept that we cannot verify the exact save contract without a real block registry — or hold at the `core/html` fallback until Q23 gives us a validator? |

| Q28 | 2026-08-05 | P3 | Fidelity | Loss reporting is pattern-driven: an attribute is reported when its *name* matches a registry entry. That catches everything Divi ships today but will silently miss a module setting whose name follows no existing pattern, and it cannot tell "set to the default" from "set deliberately". Do we invert it — enumerate the attributes each renderer consumes and report everything else — which is precise but has to be maintained per renderer? |

---

## Resolved

| ID | Raised | Resolved | Question | Decision | Implemented in |
| --- | --- | --- | --- | --- | --- |
| Q31 | 2026-08-05 | 2026-08-05 | Block validity was proven against one `@wordpress/block-library` — the newest npm publishes, which no released WordPress ships. Validate per release, or hold the claim at block *registration*? | **Validate per release.** `tests/js/versions.json` maps WordPress 6.1–7.0.2 to the `@wordpress` packages each actually shipped, derived from `wordpress-develop` at the matching tag (and, for 7.0, from the `gutenberg.sha` that release builds from). `bin/block-library-matrix.sh` converts the fixture corpus *as that version* — the plugin adapts its output to the WordPress it runs on — and validates it against that release's own library. Two things made it work at all, both recorded in `versions.json`: pinning every `@wordpress` package through npm `overrides`, because a second nested copy of `blocks`, `data` or `private-apis` throws at import time; and stubbing `matchMedia` on the jsdom *window*, which older `@wordpress/viewport` reads at import. It found two real defects on its first run — see Q33. Two fixtures remain known-invalid on 6.1 (`core/html` re-serializes through a DOM there) and are listed with reasons; a known failure that starts passing fails the run. This replaces the gap the old **Q18** note pointed at, which was the wrong ID: Q18 was about the measured `Tested up to` value. | `tests/js/versions.json`, `bin/block-library-matrix.sh`, `tests/js/validate.mjs`, `.github/workflows/tests.yml` |
| Q32 | 2026-08-05 | 2026-08-05 | Conversion could be run with the backup checkbox cleared, which still overwrote `post_content` and still deleted Divi's builder meta. Keep the choice, put it behind a second confirmation, or make the backup mandatory? | **Mandatory.** The argument for the checkbox was that revisions exist. They can be disabled by a constant, pruned by a plugin, or not reach far enough — and `_et_pb_use_builder` / `_et_pb_old_content` are not in a revision at all, so a conversion with the box cleared was one nothing could undo. The server now snapshots unconditionally; `write_backup()` already kept the first snapshot and never overwrote it, so this cannot damage an existing backup. The checkbox is replaced on the Tools screen by a statement of what happens, and the `backup` request parameter is ignored rather than removed, so an old client cannot opt out. | `block-converter-for-divi.php`, `admin/class-d2g-admin.php`, `admin/js/admin.js` |
| Q33 | 2026-08-05 | 2026-08-05 | With per-version validation in place (Q31), converted Cover blocks turned out to be invalid on 6.1–6.7 and converted Toggles invalid on 6.3. Emit one canonical markup and raise the floor, or emit per-version markup? | **Per-version markup.** Raising `Requires at least` to 6.8 would drop four supported branches over two markup details, and emitting the *older* form everywhere would rely on core's deprecations — which is exactly the "valid only through a deprecation" outcome the project already refuses. `D2G_Block_Builder::wp_at_least()` reads the running WordPress version, and two call sites use it: `cover()` orders its `<img>` and dimming `<span>` the way the running release's `save()` does (core swapped them in 6.8), and `toggle()` emits `core/details`'s `summary` *block attribute* on 6.3 alone, where the summary was an attribute rather than rich text — already escaped, because 6.3 writes it out through `RichText.Content` and the decoded title would have put a live `<script>` back on the page. It is a sibling of `D2G_Converter::block_supported()`: that asks whether a block exists, this asks what shape it has. | `includes/class-d2g-block-builder.php`, `includes/renderers/class-d2g-renderer-interactive.php` |
| Q34 | 2026-08-08 | 2026-08-08 | Q22 deleted the dead style mapper and held scope at "report, do not repair". Is a real style layer now buildable, and where does it start? | **Yes, and it starts with spacing and background colour on section, row and column.** What changed is not the idea but the evidence available for it: `tests/js/canonical.mjs` can ask core what it would have saved, and `bin/block-library-matrix.sh` can prove the answer holds on all nine supported releases. Neither existed when Q22 was answered, so the honest verdict then was "do not guess". Measured first, before any converter code: a `core/group` and a `core/column` carrying `style.spacing.padding` and `style.color.background` validate on 6.1, 6.3, 6.8 and 7.0.2 — so block supports are stable across the range and the phase was viable. The measurement also produced the two facts that make it work: core serializes background, then margin, then padding, each top/right/bottom/left with `flex-basis` last, and it omits absent sides rather than zeroing them. Divi values go through a length grammar rather than an escape, which is the N-01 lesson. Deliberately out of scope and still report-only: per-device `_tablet`/`_phone` overrides, hover, transforms, parallax, animations and `custom_css_main_element` — core has no equivalent for any of them, so that residue is the exported-stylesheet conversation, not this one. | `includes/class-d2g-block-builder.php`, `includes/renderers/class-d2g-renderer-layout.php`, `includes/class-d2g-converter.php` |
| Q22 | 2026-08-04 | 2026-08-05 | `D2G_Style_Mapper`'s style-building functions were implemented but never called. Build a real style layer, emit a companion stylesheet, or delete them? | **Delete them (option c).** *(Superseded in part by Q34: a style layer was later built, from block supports rather than from these functions — exactly the "new layer" this resolution called for.)* Nothing called `build_inline_style()`, `wrapper_style()`, `get_color_attrs()`, `parse_font()`, or the spacing and border-radius parsers; coverage measured the file at 6%. Dead is not the same as harmless — `build_inline_style()` concatenated raw Divi values into CSS declarations and appended `custom_css_main_element` verbatim, which is the injection class N-01 had to fix in the live renderers, so reconnecting it to save time would have reintroduced it. `text_align_class()` is all that remains, and it is an allowlist. If a style layer is built later it should start from block supports and strict value grammars, not from string concatenation. The documented scope holds at §5.1: loss is reported, not repaired. | `includes/class-d2g-style-mapper.php` |
| Q1 | 2026-08-04 | 2026-08-04 | Backup meta was written but never read — ship a restore UI, rely on revisions, or drop the backup? | **Ship a restore UI.** Revisions are unreliable (they can be disabled or pruned) and the backup already existed. Added a `d2g_restore_page` endpoint plus a per-row **Restore** button that also re-enables the Divi Builder. The backup meta is kept after restoring so the action is repeatable. | `block-converter-for-divi.php` (`ajax_restore_page`), `admin/js/admin.js`, v1.1.0 |
| Q2 | 2026-08-04 | 2026-08-04 | Cut accumulated work as `1.1.0` or `1.0.1`? | **`1.1.0`.** The release adds features (restore, working filters, pagination), which SemVer puts at MINOR. | `block-converter-for-divi.php` header + `D2G_VERSION`, `CHANGELOG.md` |
| Q3 | 2026-08-04 | 2026-08-04 | Implement the inert filter/sort controls, or delete the markup? | **Implement.** All three selects and the sortable headers now drive the server-side query, and the filter bar shows after a scan — including on an empty result set, so a filter matching nothing can be undone. | `admin/js/admin.js`, `admin/class-d2g-admin.php`, `admin/css/admin.css`, v1.1.0 |
| Q4 | 2026-08-04 | 2026-08-04 | Should per-page drive a POST parameter or persist to the user option? | **POST parameter**, whitelisted server-side to 20/50/100/all. `edit_per_page` remains the fallback when no valid value is sent. Persisting was rejected as surprising — it would silently change the Posts list screen too. | `block-converter-for-divi.php` (`ajax_scan_pages`), v1.1.0 |
| Q8 | 2026-08-04 | 2026-08-04 | Is the target WordPress.org, GitHub releases only, or private distribution? | **WordPress.org, in addition to GitHub releases.** Added `readme.txt` in WP.org format and a `Stable tag` guard in the build script, since WP.org serves whatever that tag names. Submission is *not* done and is blocked on Q17–Q19. GitHub releases remain the distribution channel meanwhile, so no third-party update checker is needed yet. | `readme.txt`, `bin/build-zip.sh` |
| Q17 | 2026-08-04 | 2026-08-04 | The plugin name and slug both led with a third-party trademark ("Divi"), which WP.org rejects. Rename — and how far does it reach? | **Renamed to "Block Converter for Divi" (`block-converter-for-divi`)**, using the accepted trailing-attribution form. The rename covers the public identity and everything WP.org regulates: plugin name, slug, main file, text domain, admin menu slug, and the main class. It deliberately stops short of the `D2G_` / `d2g_` internal prefixes — those are already unique, and the storage keys built from them (`_d2g_divi_backup`, `_d2g_backup_date`, `d2g_delete_data_on_uninstall`) hold live data on every 1.0.0–1.2.0 install. Renaming those would orphan every existing backup, so they stay. Follow-ups: Q20 (version number), Q21 (GitHub repo name). | `block-converter-for-divi.php`, `readme.txt`, `admin/class-d2g-admin.php`, `bin/build-zip.sh` |
| Q13 | 2026-08-04 | 2026-08-04 | Missing `uninstall.php` and `LICENSE`; on uninstall should backup meta be purged (clean) or retained (safe)? | **Both, via an opt-in setting that defaults to retain.** Purging by default would mean deleting the plugin silently destroys the only rollback path for every page it converted; retaining unconditionally would leave potentially megabytes of orphaned meta with no way to clear it. A "Delete all Divi backups when this plugin is deleted" checkbox (off by default, confirmation on enable) resolves both. The preference row itself is always removed, so a reinstall starts from the safe default. `LICENSE` is the canonical GPL-2.0 text (`md5 b234ee4d…`). | `uninstall.php`, `LICENSE`, `block-converter-for-divi.php` (`ajax_save_settings`), `admin/class-d2g-admin.php`, `admin/js/admin.js` |
| Q25 | 2026-08-05 | 2026-08-05 | The repository was private, so every public URL the plugin ships still 404'd. Publish it, host the download elsewhere, or drop the URLs? | **Published.** The repository is public. The history was scanned first — no credentials, keys, or local paths, and nothing but plugin source and documentation was ever committed. Anonymous requests to the repository, releases, and issues URLs now return 200, which is the check that failed in the review. Two consequences accepted deliberately: the review documents are now public, so the data-loss defects in 1.0.0–2.0.0 are a matter of record (the plugin was never distributed, so there is no install base to warn), and `johnjanney@gmail.com` appears in the commit history alongside the GitHub noreply address. | GitHub repository settings |
| Q27 | 2026-08-05 | 2026-08-05 | Nothing tested the AJAX endpoints, capability checks, the write lock, backup and restore state, or the database round trip. Stand up a WordPress test install — and is that the same work as Q23? | **Not the same work, and much smaller than expected.** Q23 needed a block registry, which is an npm package. This needed a database, which is `wp-env`. `tests/live/run.php` drives the real AJAX endpoints via `do_action( 'wp_ajax_…' )` with a catchable `wp_die`, and checks the contract (bad nonce, missing and stale source token, an editor without `manage_options`, lock exclusivity) plus the part no offline test can reach: every fixture inserted, converted through the endpoint, and compared byte for byte with the offline conversion after `wp_slash()`, KSES and MySQL have had it. Restore is checked for byte-identity on every fixture. `bin/live-check.sh` runs it and then feeds what the *database* holds to the block validator. Still open: multisite, and a corpus of real Divi pages. | `tests/live/`, `bin/live-check.sh`, `.wp-env.json`, v2.2.0 |
| Q18 | 2026-08-04 | 2026-08-05 | `Tested up to: 6.8` was a placeholder, not a test result, and blocked publication. Set it from a real run — on which versions? | **Set to `7.0`, from measurement.** `bin/wp-matrix.sh` runs the live suite on a clean environment per version, pairing each WordPress with a PHP it supported. Results: 6.0 **fails** (see Q29), and 6.1, 6.2, 6.3, 6.8 and 7.0.2 all pass — endpoints, database round trip, restore byte-identity, block registration, and an empty WordPress debug log on each. `Requires at least` is now 6.1 and `Tested up to` is 7.0, both from data. **What "tested" does and does not mean here:** every check is automated. Converted *content* is verified by core's own block validator, which is the same comparison that produces "unexpected or invalid content" in the editor — so that risk is genuinely retired. The admin *screen* has never been opened in a browser; that is Q30. | `bin/wp-matrix.sh`, `readme.txt`, `block-converter-for-divi.php`, v2.2.0 |
| Q15 | 2026-08-04 | 2026-08-05 | `wp_update_post()` filters content through KSES for users without `unfiltered_html`; on multisite only super admins have it, so a site administrator converting could have markup silently stripped. Require the capability, warn, or write via `$wpdb` to bypass? | **Detect the loss and refuse, naming what would go.** Measured first, on a real network with a genuine non-super-admin site administrator: a Divi Code module holding a tracking script converts to a `core/html` block, and KSES stores the JavaScript as *visible text* with the `<script>` tags removed and the `<iframe>` deleted — reported as success. Across the fixture set KSES alters 24 of 138 conversions, but 19 differ only in `<br/>` versus `<br />`, which core's validator tolerates (137/138 still validate); only 5 lose anything real. `kses_losses()` normalises that harmless difference, and when actual content would be lost the write is refused with the specific elements named. Applied to restore as well, since a backup holds the same Divi source. Bypassing KSES with `$wpdb` was rejected: the capability exists to stop users storing markup the site does not trust them with, and a plugin is not entitled to overrule it. Ordinary pages still convert for site administrators. | `block-converter-for-divi.php`, `tests/live/multisite.php`, `bin/multisite-check.sh`, v2.2.0 |
| Q30 | 2026-08-05 | 2026-08-05 | The Tools screen had never been opened in a browser. Add a Playwright run against `wp-env`, or accept manual testing and say so? | **Playwright, in a container.** `bash bin/e2e.sh` seeds the site and drives the screen: scan, preview, single convert, restore, batch, settings, filters and sorting, dialog keyboard behaviour, and console errors on load. The browser runs inside Playwright's own image because Chromium needs system libraries this project cannot assume; that also means CI and a laptop run the same thing. The central test edits one page of a batch behind the browser's back so its source token goes stale, and asserts the batch reports two converted and one failed *and names the failure* — the 2.0.0 defect. It found two real defects nothing else could see: a successful conversion wrote nothing to the `aria-live` region while failures did, and conversion warnings were returned by the server and dropped by the browser unless the user happened to preview first. Both fixed, both with a test that fails against the previous code. Not covered: pagination, per-page sizes, select-all, and batches larger than three. | `tests/e2e/`, `bin/e2e.sh`, `admin/js/admin.js`, `admin/class-d2g-admin.php`, v2.2.0 |
| Q29 | 2026-08-05 | 2026-08-05 | `Requires at least: 6.0` was derived by reading which blocks the converter emits and looking up when each arrived (Q12). Is the derivation right? | **No — the floor is 6.1, and this was measured rather than reasoned.** The live suite now asserts that every block it actually emits is registered on the WordPress under test, which is the one check that cannot be run offline: the fixture suite assumes a current install, and core's JS validator only knows the block library npm ships. On 6.0 two blocks are missing — `core/comments` (6.1, not 6.0 as Q12 claimed) and `core/list-item` (6.1, and absent from the derivation entirely), the latter meaning **every converted list** rendered as "your site doesn't include support for this block", once per item. 6.1 passes with all 32 distinct emitted blocks registered. The floor is now 6.1 in the plugin header, `readme.txt`, `README.md` and `BRIEF.md`, and the two source comments that asserted "since WordPress 6.0" are corrected. | `bin/wp-matrix.sh`, `tests/live/run.php`, v2.2.0 |
| Q23 | 2026-08-05 | 2026-08-05 | The suite could not run core's JavaScript `save()` functions, so "no block validation errors" was unproven. Stand up a real harness — and how heavy does it have to be? | **`@wordpress/blocks` + `@wordpress/block-library` under jsdom, no WordPress install.** The proposed `wp-env` route was heavier than the question needed: block validity is decided by `parse()` re-running each block's `save()` over the parsed attributes and comparing, and that is a Node package, not a database. `tests/js/validate.mjs` registers the real 113 core blocks and reports core's own verdict; `tests/lib/blockcheck.php` folds it into the PHP run so one command covers everything. It found three defects on its first runs that four rounds of static checks had missed — every converted Cover was invalid, coloured Separators were invalid, and Comments was emitted self-closing. `tests/js/canonical.mjs` was added alongside it to ask core what it *would* have saved, so hand-written block markup stops being guesswork. **This does not close Q18:** validity against the pinned block library is not the same as a given WordPress release opening the page. | `tests/js/`, `tests/lib/blockcheck.php`, `tests/run.php`, v2.2.0 |
| Q24 | 2026-08-05 | 2026-08-05 | The tokenizer stopped an opening tag at the first `]`, even inside a quoted attribute value, so `title="Array[0]"` truncated the tag. Move to a quote-aware scanner, or accept and document it? | **Quote-aware scanner.** "Accept and document" rested on the assumption that Divi does not emit such values, which was never evidenced, and the failure mode is not a graceful degradation — the leftover `"]` was published as visible text on the page. `D2G_Parser::scan_tag_end()` walks from `[` to the closing `]` treating single- and double-quoted spans as opaque, and `next_tag_span()` builds one tokenizer on top of it that the detector, the tree parser, the closing-tag matcher, and the stripper all share — so the four can no longer disagree about what a tag is. Self-closing detection moved after the scan, because `src="a/"` also ends in a slash. | `includes/class-d2g-parser.php`, v2.2.0 |
| Q21 | 2026-08-04 | 2026-08-05 | The GitHub repo was still named `divi2gutenberg` while the shipped URLs pointed at `block-converter-for-divi`. Rename it, or revert the URLs? | **Renamed.** The repository is now `github.com/johnjanney/block-converter-for-divi`, confirmed by pushing 2.1.0 to it over SSH; GitHub redirects the old paths, so nothing that referenced `divi2gutenberg` breaks. **This does not make the public links work.** The repo is private, so anonymous requests to the same URLs still 404 — the symptom Q21 was raised for survives its own resolution, and is now tracked as Q25. | GitHub repository settings |
| Q20 | 2026-08-04 | 2026-08-04 | Cut the rename as MINOR (`1.3.0`) or MAJOR (`2.0.0`)? | **`2.0.0`.** The distribution identity breaks — a different plugin directory means no automatic upgrade path — and that is what MAJOR signals, even though the code and stored data are backwards compatible. | `block-converter-for-divi.php`, `readme.txt`, `CHANGELOG.md`, v2.0.0 |
| Q6 | 2026-08-04 | 2026-08-05 | Should placeholder modules be findable, and should there be a per-page "N modules need manual attention" report? | **Yes to the report; placeholders stay as visible text.** `D2G_Converter` now collects a warning per lossy or unmapped module and `get_warnings()` returns them; the preview modal lists them above the diff, and the convert response carries them too. A `core/html` comment block was rejected — invisible on the front end is worse than a labelled paragraph the user can see and delete. Warnings also cover contact forms, menus, portfolios, gallery carousels, unrecognised modules, and tables that could not round-trip. | `includes/class-d2g-converter.php`, `admin/js/admin.js`, `admin/class-d2g-admin.php`, v2.1.0 |
| Q10 | 2026-08-04 | 2026-08-05 | Fixture-based tests under PHPUnit + `wp-env`, or a lighter standalone harness? | **Standalone harness first.** `tests/` runs on plain `php` with a ~60-line WordPress shim, no install, no Composer, no database — which means it actually gets run, and `bin/build-zip.sh` now refuses to package if it fails. It covers parser detection, per-module fixtures, and structural assertions (markup vs. block attributes, no surviving shortcodes, balanced delimiters). It found two real bugs the review had not: unrecognised `et_pb_*` modules being printed verbatim, and the depth limit leaking shortcode text. `wp-env` is still needed for the JavaScript validator half — that is Q23, not this. | `tests/`, `bin/build-zip.sh`, v2.1.0 |
| Q12 | 2026-08-04 | 2026-08-05 | Raise the WordPress minimum, or feature-detect and degrade? | ⚠️ **This resolution was wrong and was corrected in 2.2.0 — see Q29.** The minimum went to `6.0`, derived from the blocks actually emitted (`core/comments` 6.0, `core/navigation` 5.9, `core/query` 5.8) rather than picked. The derivation was wrong: `core/comments` is 6.1, not 6.0, and `core/list-item` was never considered at all. `core/details` (6.3) is feature-detected through `WP_Block_Type_Registry` and degrades to a heading plus text on 6.0–6.2. `core/form` was dropped entirely — it is experimental and unregistered in core, so it was never a target. | `block-converter-for-divi.php`, `includes/class-d2g-converter.php`, `readme.txt`, v2.1.0 |
| Q16 | 2026-08-04 | 2026-08-05 | Should the backup snapshot capture `_et_pb_*` meta rather than just `post_content`? | **Yes.** `write_backup()` captures `_et_pb_use_builder` and `_et_pb_old_content` into `_d2g_builder_meta` before conversion deletes them, and restore puts them back as found, falling back to switching the builder on for backups taken before 2.1.0. The content snapshot itself is now write-once: `_d2g_divi_backup` is never overwritten, so no later request can replace the original with converted content. | `block-converter-for-divi.php`, `uninstall.php`, v2.1.0 |
| Q9 | 2026-08-04 | 2026-08-04 | Do we need a `_d2g_converted` flag to prevent double conversion? | **No separate flag needed.** The scan now returns a `has_divi` value computed in SQL; rows without Divi content render with Preview, Convert, and their checkbox disabled. That is derived from actual content, so it cannot drift out of sync the way a stored flag would. | `block-converter-for-divi.php` (`ajax_scan_pages`), `admin/js/admin.js`, v1.1.0 |
